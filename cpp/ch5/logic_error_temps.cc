#include "std_lib_facilities.h"

int main() {
  vector<double> temps;

  for (double temp; cin >> temp; ) temps.push_back(temp);

  double sum{0};
  double hi{0};
  double lo{0};

  for (int tt : temps) {
    if HERE
    if
    sum += tt;
  }

  return 0;
}
